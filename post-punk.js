function show ( name )
{
  var elem = document.getElementById(name); 
  if ( elem ) 
    elem.style.display ="block";
}


function hide ( name )
{
  var elem = document.getElementById(name); 
  if ( elem ) 
    elem.style.display = "none";
}

function show1 ( name )
{
  var elem = document.getElementById(name); 
  if ( elem ) 
    elem.style.display ="block";
}


function hide1 ( name )
{
  var elem = document.getElementById(name); 
  if ( elem ) 
    elem.style.display = "none";
}

function show2 ( name )
{
  var elem = document.getElementById(name); 
  if ( elem ) 
    elem.style.display ="block";
}


function hide2 ( name )
{
  var elem = document.getElementById(name); 
  if ( elem ) 
    elem.style.display = "none";
    
}

function show3 ( name )
{
  var elem = document.getElementById(name); 
  if ( elem ) 
    elem.style.display ="block";
}


function hide3 ( name )
{
  var elem = document.getElementById(name); 
  if ( elem ) 
    elem.style.display = "none";
}

function show4 ( name )
{
  var elem = document.getElementById(name); 
  if ( elem ) 
    elem.style.display ="block";
}


function hide4 ( name )
{
  var elem = document.getElementById(name); 
  if ( elem ) 
    elem.style.display = "none";
}
